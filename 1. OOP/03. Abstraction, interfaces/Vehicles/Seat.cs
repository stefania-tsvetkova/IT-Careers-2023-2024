namespace Vehicles
{
    public class Seat : Car
    {
        public Seat(string model, string color)
        {
            Brand = "Seat";
            Model = model;
            Color = color;
        }
    }
}
